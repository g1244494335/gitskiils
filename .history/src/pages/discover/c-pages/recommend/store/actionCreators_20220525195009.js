import * as actionTypes from './constant'

import {getTopBanners} from "@/services/"

dispatch(getTopBannerAction())

export const getTopBannerAction = () => {
    return dispatch => {
        
    }
}